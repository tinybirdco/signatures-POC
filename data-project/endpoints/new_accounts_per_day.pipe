TOKEN "read_all_pipes_token" READ

TOKEN "new_accounts_per_day_endpoint_read_5159" READ

NODE new_accounts_per_day_0
SQL >

    SELECT 
      toDate(created_on) as Day, 
      count(account_id) as New_Accounts 
    FROM 
      accounts 
    GROUP BY 
      Day 
    ORDER BY 
      Day ASC
    LIMIT
      90


