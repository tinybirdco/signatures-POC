NODE endpoint
SQL >

    SELECT account_id, count() c 
    FROM signatures
    GROUP BY account_id
    order by c desc



NODE signatures_have_the_same_signature_id
DESCRIPTION >
    This is to test if signature event state tracking has been implimented correctly

SQL >

    SELECT signature_id, count(*) c
    FROM signatures
    GROUP BY signature_id
    order by c desc


